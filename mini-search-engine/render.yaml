services:
  - type: web
    name: mini-search-engine-api
    runtime: python
    rootDir: mini-search-engine
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
